sudo: required
services:
  - docker

branches:
  only: 
    - main

before_install:
  - docker build -t raje2012/docker-react -f Dockerfile.dev .

script:
  - docker run raje2012/docker-react npm run test -- --coverage
